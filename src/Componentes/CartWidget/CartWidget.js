import cart from './assets/cart.svg'

const CardWidget = () => {
    return (
        <div>
            <img src = { cart } style = {{ width: 25}}/>
            0
        </div>
    )
}

export default CardWidget